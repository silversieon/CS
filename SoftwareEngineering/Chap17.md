# 📌17장 분산 소프트웨어 공학

분산시스템은 단일 어플리케이션이 하나의 컴퓨터에서 수행되는 것이 아니라 다수의 컴퓨터들에서 실행되는 것.

자원공유, 개방성, 동시 수행, 확장성, 장애 내성과 같은 이점을 가진다

분산 컴퓨터에서 상호작용 방식은 절차적 상호작용과 메시지 기반 상호 작용이 있다.

- 절차적 통신은 미들웨어를 통해 원격 프로시저 호출을 하고, 호출자를 대표하는 스텁을 통해 결과를 받는다.
- 메시지 기반 통신은 요청된 서비스를 상세하게[ 정의한 메시지를 생성하여 통신한다.(보내고 기다리지 않음)

- 미들웨어: 시스템의 분산 컴포넌트 사이에 위치하여 관리, 통신, 데이터 교환을 돕는 소프트웨어이다.

### 클라이언트-서버 컴퓨팅

웹브라우저, 모바일 기기의 앱과 상호 작용하며 서비스를 제공하는 컴퓨터 방식

> 어플리케이션에 대한 계층적 아키텍처 모델
>
1. 표현 계층: 사용자에게 정보를 표현하고 모든 사용자의 상호 작용 관리
2. 데이터 처리 계층: 클라이언트로부터 전송되는 데이터를 관리, 검증, 웹 페이지 생성 등을 구현
3. 어플리케이션 처리 계층: 어플리케이션 논리의 구현과 요구된 기능을 최종 사용자에게 제공
4. 데이터베이스 계층: 데이터를 저장하고 트랜잭션 관리와 질의 서비스를 제공

### 2단 클라이언트-서버 아키텍처

단순한 클라이언트-서버 시스템으로 하나의 논리적 서버와 이를 사용하는 다수의 클라이언트로 구현

- 씬 클라이언트 모델: 표현 계층만 클라이언트에서 구현되고 나머지 계층은 서버에서 구현
- 팻 클라이언트 모델: 일부 또는 모든 어플리케이션 처리 과정이 클라이언트에서 수행(데이터 관리와 데이터베이스 기능만 서버에 구현)

### 다단 클라이언트-서버 아키텍처

시스템의 논리 계층들이 두 개의 컴퓨터 시스템에 모두 매핑되는 것(2단 X)이 아닌 여러 컴퓨터에 매핑

여러 대의 서버를 통해 어플리케이션 처리를 분산하므로 2단 아키텍처보다 확장성이 뛰어나며, 추가 서버 개설 등 클라이언트 요청에 신속 대응 가능하다.

### 분산 컴포넌트 아키텍처⭐️

미들웨어에 의존하며 상호작용하는 여러 컴포넌트들의 집합. 각 컴포넌트들은 그들이 제공하는 서비스들의 집합에 대한 인터페이스를 제공한다.

분산 컴포넌트 모델은 유연하며, 확장성이 높다. 따라서 어디서 어떻게 서비스를 제공되어야 하는지 사전에 결정할 필요가 없고, 필요에 따라 자원을 추가하기 쉽다.

- CORBA(Common Object Request Broker Architecture): **서로 다른 언어·플랫폼의 객체들이 네트워크를 통해 통신하도록 해주는 분산 객체 미들웨어 표준.(내부적으로 ORB가 통신 중개)**

- IDL(Interface Definition Language): CORBA에서 원격 객체의 인터페이스를 언어 중립적으로 정의하는 명세 언어 (각 언어별 스텁, 스켈레톤 생성을 위해)

- 마샬링: **객체 또는 데이터를 네트워크 전송이 가능한 바이트 스트림으로 변환하는 과정(스텁에서 수행)**
- 언마샬링: **전송된 바이트 스트림을 다시 원래의 객체/데이터로 복원하는 과정(스켈레톤에서 수행)**
    - 목적: 서로 다른 시스템 간 데이터 교환, 네트워크 통신을 위한 데이터 직렬화

- 스텁: 클라이언트 측에 위치한 대리 객체(Proxy)
    - 역할: 클라이언트의 메서드 호출을 가로채고, 인자를 마샬링하며, ORB를 통해 서버에 요청 전달

- 스켈레톤: 서버 측에서 클라이언트의 요청을 실제 객체로 전달하는 중개 코드
    - 역할: 요청 수신, 인자 언마샬링, 실제 서버 객체 메서드 호출, 결과를 다시 마샬링하여 반환

### 피어-투-피어 아키텍처

네트워크의 모든 노드가 주어진 연산을 수행할 수 있는 비중앙집중적인 시스템.

원칙적으로 서버와 클라이언트 사이에 어떠한 차이점도 발생하지 않는다.

어플리케이션 자체에 노드 간의 통신을 가능하게 하는 표준과 프로토콜이 내장되고, 각 노드들은 해당 어플리케이션을 실행해야 한다. ex)비트코인

> 비중앙집중 방식
>

**중앙 서버 없이 모든 노드가 동등한 역할을 수행하는 구조**

원칙적으로 서버와 클라이언트 사이에 어떠한 차이점도 발생하지 않는다.(손실, 결함 내성의 장점)

노드가 직접 서로를 탐색·연결한다.(중복 통신으로 인한 부하 발생)

> 반집중 방식
>

일반 피어를 대신해 슈퍼 피어라고 불리는 서버 역할을 하는(완전 서버X) 피어가 존재하는 구조

피어 간 연결 형성을 돕거나, 연산 결과를 조정한다.

자원 검색, 관리가 용이하지만 중앙 서버 장애시 서비스에 영향이 있으며 완전한 탈중앙 방식이 아니다.

### 서비스로서의 소프트웨어

- SaaS: 소프트웨어를 웹서비스 형태로 사용 가능하다. ex) Gmail, Notion
- PaaS: OS+런타임+개발플랫폼 제공 (어플리케이션 코드만 작성)
- IaaS: 서버, 스토리지, 네트워크 등의 인프라만 제공(하드웨어 제공)

- MSA: 애플리케이션을 독립적으로 배포·확장 가능한 작은 서비스들로 분리해 구성하는 아키텍처 스타일